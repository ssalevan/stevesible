---
authorized_keys:
  - "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"

base_group: ansibler
base_user: ansibler

logwatch_email: root@localhost

sshd_port: 22

zsh_theme: candy

# when false installs oh-my-zsh instead
zprezto: false
runcoms: "/home/{{ base_user }}/.zprezto/runcoms"
